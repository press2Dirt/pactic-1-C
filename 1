#include <stdio.h>

int main() {
    float cels, fhrngt;
// Input data
    printf("Enter celsius degrees: ");
    scanf("%f", &cels);
// Convertion
    fhrngt = cels * 9 / 5 + 32;
    printf("C: %.00f; F: %.2f", cels, fhrngt);
    return 0;
}

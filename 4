#include <stdio.h>

int main() {
// Getting num
  int n;
  printf("Введите целое число: ");
  scanf("%d", &n);
// Bit operation for checking raised to power num
  printf("%d\n", (n && !(n & (n - 1))));
  return 0;
}
